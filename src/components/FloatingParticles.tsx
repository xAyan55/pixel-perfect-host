// Simplified - no animated particles for better performance
export const FloatingParticles = ({ count = 20 }: { count?: number }) => {
  // Return null to avoid rendering any animated elements
  return null;
};
